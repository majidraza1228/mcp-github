## Example environment variables for the MCP server
# Database URL should be a SQLAlchemy URL, for example:
# PostgreSQL: postgresql+asyncpg://user:password@localhost:5432/dbname
# MySQL: mysql+aiomysql://user:password@localhost:3306/dbname
# SQLite (async, file): sqlite+aiosqlite:///./test.db

DATABASE_URL="sqlite+aiosqlite:///./mcp_example.db"
# Set to "true" to allow non-SELECT statements via `/execute` endpoint
ALLOW_WRITE="false"
PORT=8000
